var Frame = $( "#GameFrame" )[0];

function ErrorMessage() {
    alert( "These are controls for mobile devices. Currently, they do not work." );
}

function ShootFunction() {
    // alert( "boom boom" );
    // Frame.contentWindow.focus();
    // element.dispatchEvent(new KeyboardEvent('keydown', {'key': 'c'}));
    // console.log( "SHOOT" );

    ErrorMessage();
}

function JumpFunction() {
    // alert( "boing boing" );
    // console.log( "JUMP" );

    ErrorMessage();
}
